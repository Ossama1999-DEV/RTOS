// #include <xc.h>
#include "version.h"
#include <stdio.h>

int main(void) {
    const char* version = get_firmware_version();
    printf("Firmware Version: %s\n", version);
    // while(1) {
    //     // Boucle principale
        
    // }
}